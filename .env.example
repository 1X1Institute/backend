# Environment Variables Example

# Server Configuration
PORT=5001

# Database (MongoDB Example)
MONGO_URI=mongodb://localhost:27017/lxp-mvp-db # Replace with your MongoDB connection string

# Security
JWT_SECRET=your_very_strong_jwt_secret_key # Replace with a strong, random secret
JWT_EXPIRE=30d # JWT token expiration time (e.g., 30 days)

# Add other environment variables as needed (e.g., AI API keys)
# OPENAI_API_KEY=your_openai_api_key
